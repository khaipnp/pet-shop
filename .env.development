# ─── Development (next dev - chạy trên Mac) ─────────────
# Keep machine-specific DATABASE_URL credentials in the git-ignored .env file.
# Defining placeholder credentials here overrides .env during `next dev`.

# DATABASE_URL is loaded from .env
JWT_SECRET=dev-secret-do-not-use-in-prod
JWT_EXPIRES_IN=7d

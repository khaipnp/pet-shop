# ─── Production (next build / next start) ───────────────
# Các giá trị này được inject qua docker-compose hoặc Komodo
# File này chỉ để tham khảo, không dùng thật

DATABASE_URL=postgresql://petshop:***@pet-shop-db:5432/pet_shop
JWT_SECRET=change-me-to-a-real-secret
JWT_EXPIRES_IN=7d
NODE_ENV=production
HOSTNAME=0.0.0.0
